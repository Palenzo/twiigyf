# API base URL for the backend.
# Leave empty when using CRA proxy (development) or when the backend
# serves the frontend from the same origin (production single-server).
# Set to your backend URL when deploying frontend and backend separately,
# e.g.: REACT_APP_API_URL=https://your-backend.com
REACT_APP_API_URL=
